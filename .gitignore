# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Test and type caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local model weights and generated media
.model-cache/
outputs/*
!outputs/.gitkeep

# Spike artifacts. Disposable probes write here; their conclusions belong in
# research.md, not in the repository as loose files.
spike-report*.json
spike-out/
spike-*.log

# DiffSynth resolves its `local_model_path` to ./models and downloads there at
# load time unless every component is pinned. Ignored so an accidental hidden
# download is never committed -- see research.md, "A hidden network download".
models/

# Configuration
.env

# OS
.DS_Store
Thumbs.db
