# Development and test dependencies (Python 3.11).
# Runtime dependencies live in requirements.txt. PyTorch is never declared in
# either file: the platform wheel is installed separately and must not be replaced.

# The offline suite needs the control plane but NOT the model stack, so this
# file pulls in core only. `pip install -r requirements-dev.txt` is sufficient to
# run every offline test on any platform.
-r requirements-core.txt

pytest>=8.0
pytest-cov>=5.0

# Validates published manifests against contracts/request-bundle.schema.json.
# Declared rather than optional: those tests use importorskip, so without it the
# contract checks silently pass by not running, which is the worst outcome.
jsonschema>=4.23
ruff>=0.6
mypy>=1.11
